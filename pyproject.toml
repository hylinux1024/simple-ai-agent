[project]
name = "simple-ai-agent"
version = "0.1.0"
description = "一个模块化、可扩展的 AI Agent 框架"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "nest-asyncio>=1.6.0",
]
